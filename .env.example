# Copy to .env and fill values

# Mock event broadcasting (set to 1 for testing without Gateway)
MOCK_EVENTS=0

# Gemini API (for asset generation scripts only)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_OBJECT_MODEL=nano-banana
GEMINI_OBJECT_PROMPT_TEMPLATE=Pixel art object icon, top-down 2D, transparent background PNG. Object: ____ Size: 64x64 pixels. Crisp pixels, consistent palette with the room background, no text, no shadow blur.
